thisdict = {
    'name':'denis',
    'age':22
}

for x in thisdict:
    print(x)

# check if key exits in thisdict
if 'hola' in thisdict:
    print('eso')

# add items
# thisdict['eso'] = ssl
# thisdict.update({"color":'red'})